(() => {

    // strictNullCheck
    // let isActive: boolean = null;

    // true,false,undefined,null,En teoría, todos estos valores son permitidos para un valor booleano pero con las restricción de Typescrip ya no se puede.
    // console.log(isActive)
})()